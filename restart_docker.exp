#!/usr/bin/expect -f
set password "@Tony1022837069"
spawn ssh root@72.62.160.140 "cd ~/boosis-bot && docker compose down && docker compose up -d --build"
expect "password:"
send "$password\r"
expect eof
