#!/usr/bin/expect -f
set password "@Tony1022837069"
spawn ssh root@72.62.160.140 "docker exec -i boosis-db psql -U boosis_admin -d boosis_db -c 'DROP TABLE IF EXISTS candles;'"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
