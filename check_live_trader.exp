#!/usr/bin/expect -f
set password "@Tony1022837069"
set timeout 30
spawn ssh root@72.62.160.140 "cat ~/boosis-bot/src/live/LiveTrader.js | grep -A 5 'api/candles'"
expect "*?assword:*"
send "$password\r"
expect eof
