#!/usr/bin/expect -f
set password "@Tony1022837069"
set timeout 30

spawn ssh root@72.62.160.140 "docker ps && docker inspect boosis-bot | grep StartedAt"
expect "password:"
send "$password\r"
expect eof
